{{- if .Values.ui.enabled }}
apiVersion: v1
kind: Service
metadata:
  name: {{ include "agentbox.fullname" . }}-ui
  labels:
    {{- include "agentbox.ui.labels" . | nindent 4 }}
spec:
  type: {{ .Values.ui.service.type }}
  ports:
    - port: {{ .Values.ui.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "agentbox.ui.selectorLabels" . | nindent 4 }}
{{- end }}
