# Default values for agentbox
# This is a YAML-formatted file.

# Image configuration
api:
  image:
    repository: ghcr.io/sciffer/agentbox
    tag: "main-635b13f"
    pullPolicy: IfNotPresent
  env:
    DATABASE_PATH: /data/agentbox.db
    LOG_FORMAT: json
    LOG_LEVEL: info
    SERVER_PORT: "8080"
    ADMIN_PASSWORD: "admin123"
ui:
  image:
    repository: ghcr.io/sciffer/agentbox-ui
    tag: "main-635b13f"
    pullPolicy: IfNotPresent
